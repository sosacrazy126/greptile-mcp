{
  "extensions": ["ts"],
  "spec": "tests/**/*.test.ts",
  "require": ["ts-node/register"],
  "loader": "ts-node/esm",
  "timeout": 5000,
  "recursive": true,
  "reporter": "spec",
  "bail": false,
  "exit": true,
  "color": true
}